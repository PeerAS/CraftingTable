<Page
    x:Class="Minecraft_crafting_table.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Minecraft_crafting_table"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="58*"/>
            <RowDefinition Height="134*"/>
            <RowDefinition Height="149*"/>
            <RowDefinition Height="43*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="145*"/>
            <ColumnDefinition Width="393*"/>
            <ColumnDefinition Width="145*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Name="Title" HorizontalAlignment="Center" TextWrapping="Wrap" Text="Minecraft Recipes" TextAlignment="Center" FontSize="30" VerticalAlignment="Top" Height="87" Width="495" Grid.Row="0" Grid.Column="1"/>

        <Image Name="RecipeScreen" Stretch="Fill" VerticalAlignment="Top" HorizontalAlignment="Stretch" Width="600" Height="300" Source="Assets/CraftingTable.png" Grid.Row="1" Grid.RowSpan="2" Grid.Column="1"/>

        <ListView x:Name="FavoriteScreen"
                  Grid.Column="1"
                  Grid.Row="2"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ScrollViewer.VerticalScrollBarVisibility="Disabled"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollMode="Enabled"
                  ScrollViewer.VerticalScrollMode="Disabled"
                  ScrollViewer.ZoomMode="Disabled"
                  Width="Auto"
                  MaxWidth="600"
                  Height="170"
              
                  >
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Height="120" Margin="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="{StaticResource ListViewItemPlaceholderBackgroundThemeBrush}" Width="100" Height="100">
                            <Image Source="{Binding image}"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Top" Margin="10, 0, 0, 0">
                            <TextBlock Text="{Binding title}" Style="{StaticResource TitleTextStyle}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>


        <ListView x:Name="RecipeList" 
                  SelectionMode="None"
                  Grid.Column="2"
                  Grid.Row="1"
                  Grid.RowSpan="3"
                  IsItemClickEnabled="True"
                  ItemClick="RecipeList_ItemClick"
                  MaxWidth="250"
                  >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Height="120" Margin="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="{StaticResource ListViewItemPlaceholderBackgroundThemeBrush}" Width="100" Height="100" >
                            <Image Source="{Binding image}"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Top" Margin="10, 0, 0, 0">
                            <TextBlock Text="{Binding title}" Style="{StaticResource TitleTextStyle}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>
        <Button Content="Favorite" Grid.Column="1" Grid.Row="3" VerticalAlignment="Top" HorizontalAlignment="Center" Click="favoriteClick"/>

    </Grid>
</Page>
